# Python Couchbase helper

Simple Couchbase helper for Python 3.7+ adding basic functionality such as `insert`, `upsert`, `get`, `remove`, `delete` (and `..._multi` for many) as well as a `view_query` method.


